# June 포트폴리오

## 프로젝트 개요
- **프로젝트명**: June 포트폴리오
- **목적**: 나의 작업물과 프로젝트를 소개하는 개인 포트폴리오 사이트
- **핵심 기능**
  - 자기소개
  - 작업물(프로젝트) 목록
  - 작업물 상세
  - 로그인
  - 작업물 등록 / 수정 / 삭제

## 현재 구조
- `index.html` — 단일 페이지 정적 사이트. 별도 빌드 도구 없이 순수 HTML/CSS(인라인 `<style>`)로 작성됨.
- `README.md` — 프로젝트 한 줄 소개.
- 별도의 프레임워크, 패키지 매니저, 빌드 스크립트는 아직 없음.

> 참고: 로그인, 작업물 등록/수정/삭제 기능은 아직 구현되어 있지 않다. 현재는 정적 소개 페이지 단계이며, 이 기능들을 추가하려면 데이터 저장소(DB 또는 API)와 인증 방식에 대한 결정이 먼저 필요하다.

## 작업 시 참고사항
- 새 기능(예: 섹션 추가, 스타일 변경)은 `index.html` 내에서 기존 CSS 변수(`:root`의 `--bg`, `--accent` 등)와 다크 톤 디자인을 그대로 활용해 통일감을 유지한다.
- 별도 빌드 과정이 없으므로 브라우저에서 `index.html`을 직접 열어 확인 가능.
- 정적 사이트이므로 배포 시 GitHub Pages 등 정적 호스팅을 고려할 수 있음.
- 로그인/등록/수정/삭제 기능을 추가할 때는 정적 호스팅만으로는 한계가 있으므로, 백엔드(API 서버) 또는 서버리스/BaaS(예: Firebase, Supabase 등) 도입 여부를 먼저 논의하고 결정한 뒤 구조를 확장한다.
- 새로운 기술 스택이나 빌드 도구를 도입하게 되면 이 문서(`CLAUDE.md`)의 "현재 구조" 섹션을 함께 업데이트한다.
